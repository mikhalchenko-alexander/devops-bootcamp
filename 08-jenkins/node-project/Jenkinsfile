pipeline {
    agent {
        docker {
            image 'node:18.14.1-slim'
        }
    }

    stages {
        stage('Test') {
            steps {
                sh 'npm i'
                sh 'npm run test'
            }
        }
    }
}

