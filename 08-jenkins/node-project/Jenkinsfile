pipeline {
    agent any

    stages {
        stage('Pre build') {
            sh 'npm i'
        }

        stage('Test') {
            steps {
                sh 'npm run test'
            }
        }
    }
}

