pipeline {
    agent any

    stages {
        stage('Pre build') {
            steps {
                sh 'npm i'
            }
        }

        stage('Test') {
            steps {
                sh 'npm run test'
            }
        }
    }
}

