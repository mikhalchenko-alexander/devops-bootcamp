pipeline {
    agent any

    tools {nodejs "node"}

    stages {
        stage('Pre build') {
            steps {
                sh 'cd 08-jenkins/node-project/app'
                sh 'npm i'
            }
        }

        stage('Test') {
            steps {
                sh 'npm run test'
            }
        }
    }
}

